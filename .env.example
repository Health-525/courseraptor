# 正式对话需要 DeepSeek API Key；离线演示 npm run demo 不需要配置。
# 推荐首次启动时按引导加密保存，或在终端输入无参数 /key 后安全录入。
# 请勿把 Key 放进聊天消息、截图或 GitHub Issue。
DEEPSEEK_API_KEY=

# DeepSeek API 地址（可选，默认官方 https://api.deepseek.com）
# DEEPSEEK_BASE_URL=

# 模型（可选，默认 deepseek-v4-flash；可选 deepseek-v4-pro）
# RAPTOR_MODEL=deepseek-v4-flash
# 抢课功能开关（选课季设为 1 才启用抢课/盯课工具；平时留空=关闭，回到日常对话）
# RAPTOR_ENABLE_GRAB=1

# 终端界面（可选）：默认全屏卡片 UI（@ai-sdk/tui）；
# 设为 1 改用行内渲染器——输出顺着命令行往下滚，滚轮/选中复制可用
# RAPTOR_TUI_INLINE=1

# 南京工业大学教务系统凭证（可选）：留空则首次启动 raptor 时引导录入
# （AES-256-GCM 加密保存到本机 credentials.enc，不明文落盘）
# JWGL_USERNAME=
# JWGL_PASSWORD=

# Firecrawl API Key（可选）：公开通知附件的云解析兜底。
# 未配置时仍支持本地 xlsx/docx/pdf 解析；不要向云解析提交私密文件。
# FIRECRAWL_API_KEY=

# GitHub 个人访问令牌（可选）：配置后可用「发布课表日历」把整学期课表推到你名下的
# 公开仓库，手机日历 App 用订阅链接自动同步（课表一变重新发布即自动更新）。
# 1. 到 https://github.com/settings/tokens 生成令牌：
#    经典令牌勾选 repo 权限；或细粒度令牌给「内容：读写 + Pages：读写 + 管理：读写」
# 2. 粘贴到下面（ghp_ / github_pat_ 开头）
# 注意：日历发布在公开仓库里（手机才能匿名订阅），任何拿到链接的人都能看到课表安排
# GITHUB_TOKEN=

# Gitee 私人令牌（可选，国内手机订阅强烈建议配置）：
# GitHub 的订阅链接（github.io / raw.githubusercontent.com）在国内经常打不开，
# Gitee 的 raw 链接国内直连——手机在国内就订阅 Gitee 链接，稳得多。
# 1. 到 https://gitee.com/profile/personal_access_tokens 生成私人令牌（勾选 projects、user_info）
# 2. 粘贴到下面
# GITEE_TOKEN=

# QQ 官方机器人（可选，npm run qq 启动）：
# 1. 到 https://q.qq.com 用主号实名注册并创建机器人，拿到 AppID/AppSecret
# 2. 机器人能力勾选「群聊（@消息）」，单聊需用户先添加机器人为好友
# QQBOT_APP_ID=
# QQBOT_APP_SECRET=
# 激活暗号：首次在 QQ 里给机器人发这个暗号即完成授权（openid 落盘 qq-allowlist.json）
# QQBOT_PASSCODE=

# 网页正式端口（默认 3210，占用时会选择空闲端口，以终端提示为准）
# RAPTOR_WEB_PORT=3210
# 演示端口默认 3211；演示不读取 .env，需通过终端环境变量设置 RAPTOR_DEMO_PORT。

# 更新分发后台（同学端一般不用管；维护者发版用）：
# 客户端仅供维护者本地覆盖 HTTPS 后台地址；正式 zip 由 npm run publish 自动写入地址
# RAPTOR_UPDATE_SERVER=https://updates.你的域名.com
# ── 以下两项只有维护者需要（npm run publish 发版 / 服务器部署用）──
# UPDATE_SERVER_URL=https://updates.你的域名.com
# UPDATE_ADMIN_TOKEN=更新后台管理员密钥（请使用高强度随机值）
