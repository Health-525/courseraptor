# 隐私与安全说明

CourseRaptor 是个人设备上的教务助手，与南京工业大学官方无隶属、授权或背书关系。

## 数据会去哪里

| 内容 | 保存与传输 |
|---|---|
| 教务账号与密码 | 首次引导加密保存到本机 `credentials.enc`，用于登录学校；手动 `.env` 配置是明文 |
| API Key | 推荐通过首次引导或无参数 `/key` 加密保存，用于请求模型服务 |
| 提问与教务查询结果 | 为生成回答，会发送到配置的模型服务；“本地运行”不等于“数据完全离线” |
| 聊天与长期记忆 | `session.json`、`memory.json`、`data/chat-sessions.json` 等本地文件可能含个人数据 |
| 附件与生成文件 | 保存在本机缓存和 `data/generated`；读入内容可能作为回答上下文发送给模型 |
| QQ 消息 | 经过腾讯机器人平台，并写入本机历史；机器人共用本机配置的教务账号，不提供多学生身份隔离 |
| 日历发布 | 当前 GitHub/Gitee 实现发布到公开仓库，可能暴露课程、时间和地点；本地 .ics 导出无需公开 |
| 离线演示 | 不加载 `.env`、不读取个人凭证/会话、不调用学校和模型；示例会话仅存内存 |

附件的 Firecrawl 云解析是可选兜底，仅用于公开内容。天气查询和启动时更新检查也可能访问外部服务；可通过 `RAPTOR_NO_UPDATE_CHECK=1` 关闭版本检查。

本机凭证加密依赖机器与用户信息，不能防止同机同用户权限下的恶意程序读取。不要将完整使用目录发给同学，也不要把网页版暴露到公网或作为多用户共享服务。

## 分发和更新

打包脚本按明确目录/文件清单选择内容，排除 `data/`、下载、输出、日志和根目录凭证。更新保护整个 `data/`，保留个人会话、附件、成品和校历修正。代码目录中仍须避免放入任何私密文件；维护者发布前应检查最终包内容。

公共校历基础值来自源码。不要为分发校历而把某人的整个 `data/` 放进安装包。

## 使用边界

- 自动选课默认关闭，启用后会真实提交；遵守学校规则并确认目标课程。
- 请求频率应保持克制。验证码识别可通过 `RAPTOR_DISABLE_CAPTCHA_OCR=1` 停用。
- 模型回答可能有误；考试、学分要求和申请截止日期以学校原文与本人教务记录为准。
- `run_js` 的 Node.js vm 限制适合辅助计算，不是执行恶意代码的安全边界。

## 报告问题

不要在公开 Issue 贴密码、Key、Cookie、学号、真实成绩单或完整日志。普通错误使用脱敏后的最小复现。

发现安全问题时，如仓库启用了 GitHub 私密漏洞报告，请通过仓库 Security 页提交；若未启用，先开不含漏洞细节和个人数据的 Issue，请维护者提供私密联系方式。当前没有另行公布的安全邮箱或响应时限承诺。

许可证和责任范围以 [ISC LICENSE](LICENSE) 为准。
